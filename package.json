{
  "name": "root",
  "author": "Our Sci",
  "license": "GPL-3.0-only",
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "codegen": "cross-env FORCE_COLOR=1 lerna run codegen --no-prefix --stream -- --watch",
    "storybook": "cross-env FORCE_COLOR=1 lerna --scope client run storybook --no-prefix --stream",
    "chromatic": "cross-env FORCE_COLOR=1 lerna --scope client run chromatic --no-prefix --stream",
    "server:start": "cross-env FORCE_COLOR=1 lerna --scope server run start --no-prefix --stream"
  },
  "dependencies": {}
}
