  extend type Query {
    plantings(cropType: String): [Planting]
    filter(cropType: String, filter: String): [Planting]
    test: Boolean!

    # cartItems: [Launch]!
  }
  type Planting {
    id: String
    values: [PlantingValue]
  }
  type PlantingValue {
    id: String
    name: String
    value: Float
  }

  # extend type Launch {
  #   isInCart: Boolean!
  # }

  # extend type Mutation {
  #   addOrRemoveFromCart(id: ID!): [Launch]
  # }