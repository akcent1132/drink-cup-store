query EventsCard($plantingId: String!) {
    planting(id: $plantingId) {
        id
        producerName
        title
        params {
            precipitation
            temperature
            texture
            zone
        }
        events {
            id
            date
            type
        }
    }
}