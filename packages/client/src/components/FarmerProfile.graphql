

query FarmerProfile($producerId: String!) {
  producer(id: $producerId) {
    id
    code
    plantings {
      id
      producer {
        id
        code
      }
      title
      params {
        clayPercentage
        sandPercentage
      }
      events {
        id
        date
        type
        details {
          id
          name
          value
          valueList
        }
      }
    }
  }
}