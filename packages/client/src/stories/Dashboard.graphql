query RandomContent($cropType: String! = "") {
  selectedCropType @client @export(as: "cropType")
}

query PreloadData {
  allFarmOnboardings {
    farmDomain
    values {
      key
      values
    }
  }
  allPlantings {
    id
  }
}