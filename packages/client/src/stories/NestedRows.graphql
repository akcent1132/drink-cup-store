query NestedRows($cropType: String!) {
  plantings(cropType: $cropType) {
    id
    values {
      name
      value
      plantingId
      modusId
    }
    farmOnboarding {
      values {
        key
        values
      }
    }
  }
  highlightedFilterId @client 
}
